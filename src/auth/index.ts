/**
 * Modulo auth: tipi condivisi per autenticazione e gestione utenti.
 *
 * Espone i tipi di dominio (User, Player, Admin, AuthenticatedUser)
 * e i DTO richiesta/risposta degli endpoint /auth.
 */
export * from './users';
export * from './dto';
